apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-x86-deployment
  namespace: default
spec:
  replicas: 3
  selector:
    matchLabels:
      app: test-x86
  template:
    metadata:
      labels:
        app: test-x86
    spec:
      # 容忍 x86 NodePool 的 taint
      tolerations:
        - key: arch
          value: amd64
          effect: NoSchedule

      # 节点亲和性 - 确保调度到 x86 节点
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node-type
                    operator: In
                    values:
                      - x86

      containers:
        - name: test
          image: public.ecr.aws/docker/library/nginx:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: "500m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
